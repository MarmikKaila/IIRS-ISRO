"""diffcast — full VAE fine-tune + EDM latent diffusion forecaster.

Parallel to ``flowcast/``. Imports utilities from flowcast read-only.
All channel-specific paths use ``*_diff*`` suffixes to keep the existing
flowcast pipeline's artifacts untouched.
"""
